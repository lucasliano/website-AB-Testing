version: "3.9"

services:
  web:
    build: .
    environment:
      DB_URL: "sqlite:///./rf_site.db"
      APP_NAME: "RF Analyzer"
      SECRET_KEY: "change-me"
    volumes:
      - ./analytics_cli.py:/app/analytics_cli.py
    ports:
      - "80:8000"

volumes:
  db_data:
